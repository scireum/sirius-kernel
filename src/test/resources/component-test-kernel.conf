async.executor {

    test-limited {
        poolSize = 1
        queueLength = -1
    }

    test-unlimited {
        poolSize = 1
        queueLength = 0
    }

    test-parallel {
        poolSize = 2
        queueLength = -1
    }

}

cache {
    test-cache {
        verification = 5 seconds
        ttl = 10 seconds
        maxSize = 100
    }
}
