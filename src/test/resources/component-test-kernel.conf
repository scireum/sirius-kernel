async.executor {

    test-limited {
        poolSize = 1
        queueLength = -1
    }

    test-unlimited {
        poolSize = 1
        queueLength = 0
    }

    test-parallel {
        poolSize = 2
        queueLength = -1
    }

}

cache {
    test-cache {
        verification = 500 milliseconds
        ttl = 1 seconds
        maxSize = 100
    }
}
